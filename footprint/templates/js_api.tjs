;(function() {
    {% if not jquery -%}
    // const {get, post} = require('./fetch-lib.js');
    {% include "fetch-lib.js" %}
    {%- endif %}
    {{ interface }}
    window.{{ global_name }} = new {{ interface.name }}Class()
})();